# SellGPT — Лендинг для продажи ChatGPT Business

<p align="center">
  <img src="https://img.shields.io/badge/React-18-61DAFB?logo=react&logoColor=white" />
  <img src="https://img.shields.io/badge/TypeScript-5.4-3178C6?logo=typescript&logoColor=white" />
  <img src="https://img.shields.io/badge/Vite-5-646CFF?logo=vite&logoColor=white" />
  <img src="https://img.shields.io/badge/Supabase-2.49-3ECF8E?logo=supabase&logoColor=white" />
  <img src="https://img.shields.io/badge/CSS3-Custom-1572B6?logo=css3&logoColor=white" />
</p>

**SellGPT** — это современный лендинг с личным кабинетом и админ-панелью для продажи доступа к ChatGPT Business. Проект построен на React + TypeScript с использованием Supabase в качестве backend.

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** 18+ 
- **npm** 9+ или **yarn**
- Аккаунт [Supabase](https://supabase.com) (для backend)

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd gpt

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для production

```bash
# Сборка проекта
npm run build

# Предпросмотр production-сборки
npm run preview
```

---

## 📁 Структура проекта

```
gpt/
├── index.html                 # Главная страница (base-версия)
├── index-sales.html          # Sales-версия (агрессивный копирайт)
├── dashboard.html            # Личный кабинет с админ-панелью
├── account.html              # Альтернативный вход в ЛК
├── terms.html                # Условия использования
├── privacy.html              # Политика конфиденциальности
├── src/
│   ├── App.tsx              # Главный компонент приложения
│   ├── main.tsx             # Точка входа
│   ├── styles.css           # Глобальные стили
│   └── supabaseClient.ts    # Настройка Supabase клиента
├── supabase/
│   └── schema.sql           # SQL-схема базы данных
├── package.json
├── vite.config.ts
└── README.md
```

---

## 🛠️ Технологический стек

### Frontend
| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 18.2.0 | UI библиотека |
| **TypeScript** | 5.4.5 | Типизация |
| **Vite** | 5.2.0 | Сборщик и dev-сервер |
| **CSS3** | — | Кастомные стили без фреймворков |

### Backend
| Технология | Назначение |
|------------|------------|
| **Supabase** | База данных PostgreSQL + Auth |
| **Row Level Security (RLS)** | Безопасность данных |

### Шрифты
- **Space Grotesk** — заголовки
- **IBM Plex Sans** — основной текст
- **IBM Plex Mono** — терминал и код

---

## ⚙️ Настройка Supabase

### 1. Создание проекта

1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь завершения инициализации

### 2. Получение ключей

Перейдите в **Project Settings → API**:
- **Project URL** → `VITE_SUPABASE_URL`
- **anon/public** ключ → `VITE_SUPABASE_ANON_KEY`

### 3. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 4. Применение SQL-схемы

В SQL Editor Supabase выполните содержимое файла `supabase/schema.sql`:

```sql
-- Таблица профилей пользователей
create table if not exists public.profiles (
  id uuid primary key references auth.users on delete cascade,
  email text,
  full_name text,
  avatar_url text,
  role text not null default 'user',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Таблица заявок (leads)
create table if not exists public.leads (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz not null default now(),
  name text not null,
  email text not null,
  telegram text,
  seats integer not null default 1,
  company text,
  goal text,
  status text not null default 'new',
  notes text,
  assigned_to uuid references public.profiles(id),
  source text,
  last_contacted_at timestamptz
);

-- Триггеры и функции...
```

### 5. Настройка Auth (Google OAuth)

1. В Supabase перейдите в **Authentication → Providers**
2. Включите **Google**
3. Настройте Google OAuth credentials:
   - Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
   - Включите Google+ API
   - Создайте OAuth 2.0 credentials
   - Добавьте Authorized redirect URI: `https://your-project.supabase.co/auth/v1/callback`

### 6. Назначение роли admin

После первого входа пользователя, выполните в SQL Editor:

```sql
update public.profiles 
set role = 'admin' 
where email = 'your-email@example.com';
```

---

## 📱 Страницы приложения

| Страница | URL | Описание |
|----------|-----|----------|
| **Главная (base)** | `/index.html` | Стандартная версия лендинга |
| **Главная (sales)** | `/index-sales.html` | Агрессивная версия с urgency |
| **Личный кабинет** | `/dashboard.html` | Полноценный ЛК с админ-панелью |
| **Условия** | `/terms.html` | Terms of Service |
| **Конфиденциальность** | `/privacy.html` | Privacy Policy |

---

## 🎨 Варианты лендинга

Проект поддерживает два варианта копирайта:

### Base (`index.html`)
- Мягкий, информационный тон
- Фокус на преимуществах
- Подходит для органического трафика

### Sales (`index-sales.html`)
- Агрессивный копирайт
- Элементы urgency (ограниченное количество)
- Подходит для рекламного трафика

---

## 🔐 Безопасность

### Row Level Security (RLS)

| Таблица | Политика | Описание |
|---------|----------|----------|
| `profiles` | Владелец видит только свои данные | Пользователи не видят чужие профили |
| `leads` | Любой может создать | Открытая форма заявки |
| `leads` | Только admin может читать/обновлять | Защита данных заявок |

### Аутентификация
- Google OAuth через Supabase Auth
- Автоматическое создание профиля при входе
- Сессии с автоматическим обновлением токена

---

## 🚀 Деплой на Vercel

### 1. Подготовка

Убедитесь, что у вас:
- Аккаунт на [Vercel](https://vercel.com)
- Подключен репозиторий GitHub/GitLab

### 2. Импорт проекта

1. На Vercel нажмите **Add New → Project**
2. Выберите ваш репозиторий
3. Настройте параметры:

```
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
```

### 3. Переменные окружения

Добавьте Environment Variables:

```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 4. Деплой

Нажмите **Deploy** и дождитесь завершения сборки.

### 5. Настройка домена (опционально)

1. Перейдите в **Project Settings → Domains**
2. Добавьте свой домен
3. Настройте DNS записи по инструкции Vercel

---

## 📊 Функционал админ-панели

После входа как **admin** в личном кабинете доступно:

### 📈 Статистика
- Общее количество заявок
- Распределение по статусам (новые/в работе/закрыты)

### 🔍 Поиск и фильтрация
- Поиск по имени, email, Telegram, компании
- Фильтрация по статусу

### 📤 Экспорт данных
- Экспорт в CSV
- Учитывает текущие фильтры поиска

### 🗑️ Управление заявками
- Просмотр деталей заявки
- Изменение статуса
- Добавление заметок
- Удаление заявок (одиночное и массовое)

---

## 📝 Скрипты npm

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера (Vite) |
| `npm run build` | Сборка для production |
| `npm run preview` | Предпросмотр production-сборки |

---

## 🐛 Отладка

### Проблема: "Supabase не настроен"

**Решение:**
1. Проверьте файл `.env`
2. Убедитесь, что переменные начинаются с `VITE_`
3. Перезапустите dev-сервер

### Проблема: Нет доступа к заявкам

**Решение:**
1. Войдите через Google
2. В SQL Editor выполните:
```sql
update public.profiles set role = 'admin' where email = 'ваш-email';
```

### Проблема: Ошибка RLS

**Решение:**
Убедитесь, что SQL-схема применена полностью, включая политики RLS.

---

## 📄 Лицензия

MIT License — свободное использование и модификация.

---

## 👨‍💻 Автор

Создано для продажи ChatGPT Business доступа.

---

## 📞 Поддержка

По вопросам обращайтесь в Telegram: [@imnotsheikh](https://t.me/imnotsheikh)
